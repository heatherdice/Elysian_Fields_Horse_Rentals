import React from "react";
import { AdminPage } from "../styles/admin.styles";

export default function admin() {
    <AdminPage>
        {/* Considering making a separate app for admin page/operations. Need to do more reading on what the best option here would be. */}
    </AdminPage>
}